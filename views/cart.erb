<table class="table">
  <tbody>
    <% @products.each do |product| %>
    <tr id="cart_<%= product.id %>" style="display: none;">
      <td><img src="<%= product.path_to_image %>"/></td>
      <td><%= product.title %></td>
      <td>Price for 1 item:
        <span id="price_<%= product.id %>"><%= product.price %></span>
        UAH</td>
      <td id="in_cart_for_product_<%= product.id %>"></td>
      <td id="amount_of_items_<%= product.id %>"></td>
    </tr>
    <% end %>
  </tbody>
</table>

<form>
  <div class="form-row">
    <div class="form-group col-md-4">
      <label for="inputName">Name</label>
      <input type="email" class="form-control" id="inputName" placeholder="Name">
    </div>

    <div class="form-group col-md-4">
      <label for="inputPhone">Phone</label>
      <input type="password" class="form-control" id="inputPhone" placeholder="Phone">
    </div>

    <div class="form-group col-md-4">
      <label for="inputEmail">Email</label>
      <input type="email" class="form-control" id="inputEmail" placeholder="Email">
    </div>

    <div class="form-group col-md-9">
      <label for="inputAddress">Address</label>
      <input type="text" class="form-control" id="inputAddress" placeholder="Region, city, street, house number">
    </div>

    <div class="form-group col-md-3">
      <label for="inputZip">Zip</label>
      <input type="text" class="form-control" id="inputZip">
    </div>
  </div>

  <button type="submit" class="btn btn-primary">Buy</button>
</form>
